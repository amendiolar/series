import React from 'react';

function SerieCard ({ results }){



    render(){
        return(
            <div className="info-container">
                {results.map((serie) =>{
                    
                })}
            </div>
        )
    }
}

export default SerieCard;